# vectacore-Bill-Book-Frontend

React + TypeScript + Vite frontend for the Vectacore Bill Book application.

## Scripts

```bash
npm install
npm run dev
npm run build
```

The app expects the backend API URL from `VITE_API_URL`. In local development it defaults to `http://127.0.0.1:8001/api/v1`.
